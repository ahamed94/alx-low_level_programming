#include "main.h"

/**
 * _strstr - find str in string
 * @haystack: ptr to str to look in
 * @needle: str to find
 *
 * Return: ptr to str
 */

char *_strstr(char *haystack, char *needle)
{
	return (strstr(haystack, needle));
}
