#include "main.h"

/**
 * _strcmp - Compares 2 strings
 * @s1: First string to use to compare
 * @s2: 2nd string to look in
 *
 * Return: 0 if match, less If less and more if more
 */

int _strcmp(char *s1, char *s2)
{
	return (strcmp(s1, s2));
}
