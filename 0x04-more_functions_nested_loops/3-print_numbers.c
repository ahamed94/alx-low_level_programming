#include "main.h"

/**
 * print_numbers - prints from 0~9
 * Return: Returns 1 if upper, else 0
 */

void print_numbers(void)
{
	int i;

	for (i = 0; i < 10; i++)
		printf("%d", i);

	printf("\n");
}
