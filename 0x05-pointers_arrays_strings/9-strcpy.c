#include "main.h"

/**
 * _strcpy - Copies str
 * @dest: pointer to des string
 * @src: string to be copied
 *
 * Return: Pointer to destination
 */

char *_strcpy(char *dest, char *src)
{
	strcpy(dest, src);
	return (dest);
}
